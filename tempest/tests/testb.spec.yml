
name: "test b spec"
describe:
  - name: "test 1"
    description: "a simple test"
    test:
      route: "/test/hello?status=200"
      verb: "GET"
      assert:
        - "headers[\"content-type\"] == \"application/json\""

  - name: "test 2"
    description: "another simple test"
    test:
      route: "/test/hello?status=404"
      verb: "GET"
      assert:
        - "json[0] == \"status=404\""

  - name: "new section"
    describe:
      - name: "test 1"
        description: "a simple test"
        test:
          route: "/test/hello?status=200"
          verb: "GET"
          assert:
            - "headers[\"content-type\"] == \"application/json\""

      - name: "test 2"
        description: "another simple test"
        test:
          route: "/test/hello?status=404"
          verb: "GET"
          assert:
            - "json[0] == \"status=404\""